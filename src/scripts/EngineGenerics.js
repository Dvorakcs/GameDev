class EngineGenerics{
    constructor(){
        this.isCollision = false
    }

    START(event){

    }
    UPDATE(event){

    }
    DRAW(event){
        event.context.fillStyle = "white";
        event.context.fillRect(0, 0, event.canvas.width, event.canvas.height);
    }
    STOP(event){

    }

    

  
}